loop wait is used for parallel programming, mana response anedi sync lo jaragalante idi use cheyyochu.

const loop = require('loop-wait')

loop.foreach(element=>{
//code

loop.next();
})

loop.on(err =>{
res.send(Err)
})

res.send(res)
